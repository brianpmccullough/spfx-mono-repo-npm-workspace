{
  "name": "@spfx-monorepo/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript utilities for SPFx workspace packages.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "typescript": "~5.8.0"
  }
}
